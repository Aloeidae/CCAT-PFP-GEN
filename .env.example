# Runware API key. Vite embeds VITE_-prefixed vars into the client bundle,
# so this key is PUBLIC once deployed. Set a spending limit in the Runware
# dashboard to protect it.
VITE_RUNWARE_API_KEY="your_runware_api_key_here"
